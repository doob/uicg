{
  "style": "default",
  "tailwind": {
    "config": "tailwind.config.mjs"
  },
  "aliases": {
    "components": "{{ aliasComponents }}"
  },
  "output": {
    "components": "{{ outputComponents }}"
  }
}
