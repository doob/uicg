{
  "style": "default",
  "tailwind": {
    "config": "tailwind.config.ts"
  },
  "aliases": {
    "components": "{{ aliasComponents }}"
  },
  "output": {
    "components": "{{ outputComponents }}"
  }
}
